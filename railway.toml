[build]
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/up"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Persistent volume for uploaded files (photos)
[[deploy.volumes]]
mountPath = "/data"
