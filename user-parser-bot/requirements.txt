# ── Telethon version ──
telethon>=1.34,<2.0

# ── Pyrogram version ──
pyrogram>=2.0,<3.0
tgcrypto>=1.2            # optional but speeds up Pyrogram significantly

# ── shared ──
python-dotenv>=1.0
geopy>=2.0,<3.0
anthropic>=0.1,<1.0

# ── NLP & Extraction ──
geopy>=2.0,<3.0
natasha>=1.4
pymorphy2>=0.9
pymorphy2-dicts-uk>=2.4
transliterate>=1.10

# ── Optional (AI-enhanced) ──
# Uncomment to enable transformer NER models
# transformers>=4.30
# torch>=2.0  # Install via: pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Optional (LLM) ──
# Uncomment to enable Ollama integration
# ollama>=0.1

natasha>=1.0,<2.0
transliterate>=1.0,<2.0
pymorphy2>=0.9,<1.0
pymorphy2-dicts-uk>=2.0,<3.0